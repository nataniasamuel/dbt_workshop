version: 2

models:
  - name: stg_orders
    description: "Cleaned e-commerce orders from TheLook dataset."
    columns:
      - name: order_id
        description: "Primary key of the order."
        tests:
          - unique
          - not_null

  - name: fct_orders
    description: "Orders joined with revenue from order items."